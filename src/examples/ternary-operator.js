// example 1: find largest number between 2 numbers using ternary operator
const num1 = 20;
const num2 = 10;

const result1 = num1 > num2 ? num1 : num2;
console.log(result1);

// example 2: check even/odd number using ternary operator
const number = 20;
const result2 = number % 2 === 0 ? 'even' : 'odd';
console.log(result2);
